# تشغيل ومعاينة مشروع مدى إنماء

خطوات بدء تشغيل خادم التطوير لمعاينة الموقع محلياً:

1. تأكد من تهيئة ملف المتغيرات البيئية `.env` بالقيم الصحيحة.
2. ابدأ تشغيل خادم التطوير:
   ```bash
   npm run dev
   ```
3. افتح المتصفح وتوجه إلى الرابط المحلي:
   `http://localhost:3000`
